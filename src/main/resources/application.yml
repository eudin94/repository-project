server:
  port: 8080
  servlet.context-path: '/repository-test'
  compression.enabled: true
  error.include-message: always

spring:
  application.name: repository-test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/repository_test
    username: root
    password: senha123
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true